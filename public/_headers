# 安全头与缓存策略（Cloudflare Pages / Netlify 格式）
#
# ⚠️ 平台兼容性（2026-09 实测）：
# 当前托管在 GitHub Pages（sanshuibot.github.io），GitHub Pages **不支持 _headers**，
# 本文件在线上不生效——实测响应头仅有 GitHub 自带的 HSTS 与 Cache-Control: max-age=600。
# 若迁移到 Cloudflare Pages / Netlify，本文件将自动生效，无需改动。
# 在 GitHub Pages 上如需安全头，须改用自定义 GitHub Actions 部署（改写响应头）。
#
# CSP 说明：
#  - script-src 'unsafe-inline'：根布局防 FOUC 内联 bootstrap 脚本所需。
#  - frame-src / connect-src giscus.app：评论系统（Post/PostComments.tsx）。
#  - img-src https:：giscus 评论头像来自 GitHub 域；未接入任何统计（无 GA/gtag），
#    故不放行 googletagmanager.com——接入统计时再加回。
#
/*
  Content-Security-Policy: default-src 'self'; script-src 'self' 'unsafe-inline' https://giscus.app; style-src 'self' 'unsafe-inline'; img-src 'self' data: https:; font-src 'self'; connect-src 'self' https://giscus.app; frame-src https://giscus.app; base-uri 'self'; form-action 'self'
  Strict-Transport-Security: max-age=31536000; includeSubDomains; preload
  X-Content-Type-Options: nosniff
  X-Frame-Options: DENY
  Referrer-Policy: strict-origin-when-cross-origin
  Permissions-Policy: camera=(), microphone=(), geolocation=(), browsing-topics=()
  Cache-Control: public, max-age=600

/_next/static/*
  Cache-Control: public, max-age=31536000, immutable
